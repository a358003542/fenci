## CHANGELOG

### 0.4.0
模型文件现在统一为一个文件： `fenci_model` ，不过提供了一个接口`seg.reset_model("simple")` 可以把模型文件回滚到本项目0.3.4的状态。

个人测试训练数据 `msr_training.utf8` 结果就是hmm训练几乎没有提升（也可能是原有HMM数据就是基于该语料的？），但如果将该语料数据增加到词库中，正确率是能够提高两到三个百分点的。

现在项目默认的模型文件就是基于上述操作得来的，模型文件体积增加到4MB，处理速度测试几乎没有变化，如果打开模型文件会发现一些小频率的杂七杂八的词语属于垃圾数据，但是效果就是比之前看上去很干净的那个模型要好。

NOTICE： 现在用户自定义词库后面的频次计数最好增大一点，比如计数1可能效果不会太明显了。


### 0.3.4
`from pkg_resources import resource_filename` 用法移除

### 0.3.3
`from pkg_resources import resource_filename` 用法移除

### 0.3.2
移除原线程锁，使用文件锁，多进程多线程对于模型的共用问题都能很好地解决。

### 0.3.1
更安全的文件写方式

### 0.3.0

一番整理，现在本模块不再依赖其他模块。

### 0.2.2

使用简化版本的simple_nltk。

### 0.2.0
彻底脱离原jieba分词项目结构，整体重新设计。

### 0.1.2
加入HMM训练，重新训练数据，原字典数据较小。
